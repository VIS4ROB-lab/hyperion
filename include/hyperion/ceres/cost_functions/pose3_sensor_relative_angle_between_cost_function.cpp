// -----------------------------------------------------------------------------
// This file was autogenerated by symforce from template:
//     COST_FUNCTION.cpp.jinja
// Do NOT modify by hand.
// -----------------------------------------------------------------------------

#include "pose3_sensor_relative_angle_between_cost_function.hpp"

#include <sym/ops/storage_ops.h>
#include <sym/util/epsilon.h>

#include "sym/pose3_sensor_relative_angle_between_factor.hpp"
#include "sym/pose3_sensor_relative_angle_between_factor_with_jacobians0134.hpp"

namespace hyperion::ceres {

using namespace sym_ceres;

auto Pose3SensorRelativeAngleBetweenCostFunction::Evaluate(double const* const* parameters,
                                                           double* residuals,
                                                           double** jacobians) const -> bool {
  // Create aliases.
  const auto _x = sym::StorageOps<sym::Pose3<double>>::FromStorage(parameters[0]);
  const auto _x_T_a = sym::StorageOps<sym::Pose3<double>>::FromStorage(parameters[1]);
  const auto _y = sym::StorageOps<sym::Pose3<double>>::FromStorage(parameters[2]);
  const auto _y_T_b = sym::StorageOps<sym::Pose3<double>>::FromStorage(parameters[3]);

  // Evaluation without Jacobians.
  if (!jacobians) {
    Eigen::Map<Eigen::Matrix<double, 1, 1>>{residuals} =
        Pose3SensorRelativeAngleBetweenFactor<double>(_x, _x_T_a, a_d_b_, _y, _y_T_b, sqrt_info_,
                                                      sym::kDefaultEpsilon<double>);
    return true;
  }

  // Evaluation with Jacobians.
  Eigen::Map<Eigen::Matrix<double, 1, 1>>{residuals} =
      Pose3SensorRelativeAngleBetweenFactorWithJacobians0134<double>(
          _x, _x_T_a, a_d_b_, _y, _y_T_b, sqrt_info_, sym::kDefaultEpsilon<double>, jacobians[0],
          jacobians[1], jacobians[2], jacobians[3]);
  return true;
}

}  // namespace hyperion::ceres
